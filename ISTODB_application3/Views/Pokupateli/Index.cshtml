@model IEnumerable<ISTODB_application3.Models.POKUPATELI>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            IMJA_POKUPATELJA
        </th>
        <th>
            HARAKTERISTIKI
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IMJA_POKUPATELJA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HARAKTERISTIKI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
